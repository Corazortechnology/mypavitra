import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Image Review — MyPavitra",
  robots: { index: false, follow: false },
};

export { default } from "./page-client";
