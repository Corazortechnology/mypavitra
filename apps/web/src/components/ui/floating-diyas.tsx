/**
 * Ambient floating emoji diyas — retired from the motion language.
 * Kept as a no-op export so older imports do not break during cleanup.
 */
export function FloatingDiyas(_props?: { className?: string; count?: number }) {
  return null;
}
